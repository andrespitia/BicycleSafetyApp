<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andres.bicyclesafetyapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Datos"
            android:label="@string/datos"></activity>
        <activity
            android:name=".GenerarQR"
            android:label="@string/generarQR" />
        <activity
            android:name=".Home"
            android:label="@string/home" />
        <activity
            android:name=".RegistrarBicicleta"
            android:label="@string/regbici" />
        <activity
            android:name=".Perfil"
            android:label="@string/perfil" />
        <activity
            android:name=".Escanear"
            android:label="@string/escanear" />
        <activity
            android:name=".Registrarse"
            android:label="@string/registrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>